apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docket-monster-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-path
